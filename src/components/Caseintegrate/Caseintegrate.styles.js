import styled from 'styled-components'
import { lg, md } from '../../config/variables'
export const CaseintegrateStyles = styled.div`
  margin-bottom: 94px;
  ${lg(`   
      margin-bottom: 67px;
  `)}
  ${md(`   
      margin-bottom: 40px;
  `)} 
  > .container {
    max-width: 947px;
  }
`
